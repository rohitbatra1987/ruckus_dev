# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/10-Routing

# default rules
homepage:
  url:   /
  param: { module: book, action: index }

# generic rules
# please, remove them by adding more specific rules
default_index:
  url:   /:module
  param: { action: index }

default:
  url:   /:module/:action/*
  
my_ruckus:
  url: /my_ruckus
  param: { module: my_ruckus, action: index }
error_404:
  url: /error_404
  param: { module: error_404, action: index }
